import BenefitsLayout from "@/src/modules/loyalty/components/BenefitsLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <BenefitsLayout>{children}</BenefitsLayout>;
}
